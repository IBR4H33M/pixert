<resources xmlns:tools="http://schemas.android.com/tools">
  <!--
    Android 15 (API 35) enforces edge-to-edge by default.
    statusBarColor, navigationBarColor, and
    LAYOUT_IN_DISPLAY_CUTOUT_MODE_SHORT_EDGES / DEFAULT are deprecated.
    We remove them here so the system uses the new defaults.
  -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">false</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
  </style>
</resources>
